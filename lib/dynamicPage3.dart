import 'package:flutter/material.dart';

class DynamicPage3 extends StatelessWidget {
  const DynamicPage3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nani'),
      ),
    );
  }
}
